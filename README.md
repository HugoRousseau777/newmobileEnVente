Download the project folder and open it in VSC Open a terminal in VSC and type : 
cd backend
npm install
npm run start

Open the folder in another window terminal and type 
cd front-end
npm install
npm start 

Open the url displayed in the last terminal and you'll have access to the application
